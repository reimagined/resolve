import React from 'react'
import { View } from 'react-native'

export const connectViewModel = () => Component => Component
export const connectReadModel = () => Component => Component
export const Providers = ({ children }) => <View>{children}</View>
