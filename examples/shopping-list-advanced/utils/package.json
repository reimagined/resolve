{
  "name": "@shopping-list-advanced/utils",
  "version": "0.17.2",
  "private": true,
  "scripts": {
    "build": "echo || exit 0",
    "test": "echo || exit 0",
    "test:functional": "echo || exit 0"
  }
}
