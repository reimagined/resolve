{
  "name": "@react-native-shopping-list/utils",
  "version": "0.15.2",
  "private": true,
  "scripts": {
    "build": "echo || exit 0",
    "test": "echo || exit 0",
    "test:functional": "echo || exit 0"
  }
}