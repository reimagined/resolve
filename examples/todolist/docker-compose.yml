version: '3'
services:
  testcafe:
    build:
      context: .
      dockerfile: testcafe.dockerfile
    command:
      - firefox
      - /tests/testcafe
    links:
      - todolist
    depends_on:
      - todolist
    environment:
      - HOST=todolist
  todolist:
    build: .
