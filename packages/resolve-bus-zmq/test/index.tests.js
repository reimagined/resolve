import sinon from 'sinon';
import { expect } from 'chai';
import zeromq from 'zeromq';
import adapter from '../src';

describe('ZeroMQ bus', () => {
    beforeEach(() => {
    });

    afterEach(() => {
    });

    it('', () => {
    });
});
