{
    "port": ${PORT},
    "storageAdapter": {
        "module": "resolve-$STORAGE_ADAPTER",
        "options": $STORAGE_OPTIONS
    },
    "jwtCookie": {
        "name": "$JWT_COOKIE_NAME",
        "maxAge": 123
    }
}
