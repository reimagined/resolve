{
  "dev": {
    "describe": "Set `webpack.mode` and `process.env.NODE_ENV` to 'development'",
    "type": "boolean"
  },
  "prod": {
    "describe": "Set `webpack.mode` and `process.env.NODE_ENV` to 'production'",
    "type": "boolean"
  },
  "test": {
    "describe":
      "Set `webpack.mode` to 'development' and process.env.NODE_ENV to 'test'",
    "type": "boolean"
  },
  "watch": {
    "describe": "Watch the filesystem for changes and update the app using Webpack HMR",
    "type": "boolean"
  },
  "start": {
    "describe":
      "Automatically start your server once the Webpack build completes",
    "type": "boolean"
  },
  "inspect": {
    "describe": "Start Node.js inspector on [host:]port",
    "type": "string"
  },
  "host": {
    "describe": "Application hostname",
    "type": "string"
  },
  "port": {
    "describe": "Application port",
    "type": "number"
  },
  "protocol": {
    "describe": "Application protocol",
    "type": "string"
  },
  "openBrowser": {
    "describe": "Open the root page in the default browser",
    "type": "boolean"
  },
  "browser": {
    "describe": "Specified browser for TestCafe",
    "type": "string"
  },
  "config": {
    "describe": "Path to reSolve config",
    "type": "string"
  },
  "buildConfig": {
    "describe": "Path to reSolve build config",
    "type": "string"
  },
  "rootPath": {
    "describe": "Application root path",
    "type": "string"
  },
  "registry": {
    "describe": "ReSolve registry URL",
    "type": "string"
  },
  "printConfig": {
    "describe": "Show the current configuration",
    "type": "boolean"
  }
}
