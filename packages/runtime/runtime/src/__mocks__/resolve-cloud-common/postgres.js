export const executeStatement = jest.fn().mockImplementation(() => [])
export const escapeStr = jest.fn().mockImplementation((str) => str)
export const escapeId = jest.fn().mockImplementation((str) => str)
