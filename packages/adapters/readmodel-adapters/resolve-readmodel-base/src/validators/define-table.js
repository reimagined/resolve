import maybeThrowError from './maybe-throw-error'

const defineTable = async (readModelName, tableName, tableDescription) => {
  const errors = []
  maybeThrowError(errors)
}

export default defineTable
