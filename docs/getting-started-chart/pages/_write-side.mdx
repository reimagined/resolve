**Write Side**

An application's **write side** handles commands, validates input data, and emits **events** based on valid commands. The framework then saves the emitted events to the **event store**. The write side is defined as a set of aggregates.

<!--

The chart below illustrates the reSolve write side's architecture.

![Write Side](../static/write-side-chart.png) -->
