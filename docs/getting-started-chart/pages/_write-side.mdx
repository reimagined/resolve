**Write Side**

An application's write side handles commands, validates input data, and emits events based on valid commands. The framework then saves the emitted events to the event store. The write side is defined as a set of [aggregates](../../write-side.md#aggregates).

**See Also:**

- [CQRS in reSolve](#cqrs-in-resolve)
- [Write Side](../../write-side.md)
